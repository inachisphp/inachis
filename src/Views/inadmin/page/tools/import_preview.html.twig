{% extends "inadmin/structure/main.html.twig" %}

{% block pagetitle %}
    <a href="{{ path('incc_tools_index') }}">Tools</a>
    <a href="{{ path('incc_tools_import') }}">Import</a>
    <span>Preview</span>
{% endblock pagetitle %}

{% block content %}
<div class="col-12 widget-posts">
<section class="paginated-list">
    <p>{{ pages|length }} pages detected in file. Imported content will be created with status 'draft'.</p>

    <form class="form nested-grid" method="post" action="{{ path('incc_tools_import_process') }}">
        <table class="table table--responsive table--striped table--compact col-8">
            <thead>
                <tr>
                    <th>Title</th>
                    <th>Type</th>
                    <th>Categories</th>
                    <th>Tags</th>
                    <th>Post Date</th>
                </tr>
            </thead>
            <tbody>
            {% for page in pages %}
                <tr>
                    <td>{{ page.title }}</td>
                    <td>{{ page.type }}</td>
                    <td>{{ page.categories|map(c => c.path)|join(', ')|replace({'/': ' > '}) }}</td>
                    <td>{{ page.tags|map(t => t.title)|join(', ') }}</td>
                    <td>{{ page.postDate ? page.postDate|date('Y-m-d H:i') : '' }}</td>
                </tr>

                {% if warnings[loop.index0] is defined %}
                <tr class="import-warning-row">
                    <td colspan="6">
                        <ul class="import-warnings">
                            {% for warning in warnings[loop.index0] %}
                                <li>{{ warning }}</li>
                            {% endfor %}
                        </ul>
                    </td>
                </tr>
                {% endif %}
            {% endfor %}
            </tbody>
        </table>

        <fieldset class="col-4">
            <legend>Options</legend>
            <p>Select options for importing the content:</p>
            <p>
                <label>
                    <input type="checkbox" class="checkbox"name="createMissingCategories" value="1" checked>
                    Create missing categories
                </label>
            </p>
            <p>
                <label>
                    <input type="checkbox" class="checkbox" name="createMissingTags" value="1" checked>
                    Create missing tags
                </label>
            </p>
            <p>
                <label>
                    <input type="checkbox" class="checkbox" name="overridePostDates" value="1" checked>
                    Override post dates
                </label>
            </p>
        </fieldset>

        <div class="fixed-bottom-bar">
            <p>
                <button class="button button--positive" type="submit">
                    <span class="material-icons" aria-hidden="true">sync</span>
                    <span class="button--text">Process Import</span>
                </button>
            </p>
        </div>
    </form>
</section>
</div>
{% endblock content %}
