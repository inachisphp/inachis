{{ include('inadmin/partials/tabs.html.twig', {
    tabs: [
        canResource('USER') ? {
            id: 'users',
            title: 'Users',
            url: path('incp_admin_list'),
        } : null,
        can('ROLE', 'MANAGE') ? {
            'id': 'roles',
            'title': 'Roles',
            'url': path('incp_admin_role_index'),
        } : null,
        can('AUDIT_LOG', 'VIEW') ? {
            id: 'audit-logs',
            title: 'Audit Logs',
            url: path('incp_admin_login_activity_index'),
        } : null,
    ]|filter(v => v is not null)
}) }}
