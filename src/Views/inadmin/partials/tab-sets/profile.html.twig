{{ include('inadmin/partials/tabs.html.twig', {
    displayTabsCondition: user.username is defined and user.username is not empty,
    page: { tab: activeTab, },
    tabs: [
        {
            id: 'profile',
            title: 'Profile',
            url: path('incc_admin_edit', { 'id': user.username } ),
        },
        {
            id: 'change-password',
            title: 'Change Password',
            url: path('incc_admin_change_password', { 'id': user.username }),
            displayCondition: app.user.username == user.username,
        }
    ]
}) }}
<!-- Two-factor authentication -->
<!-- Access -->
